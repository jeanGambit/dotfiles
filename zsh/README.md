# ZSHELL
Here are default zsh files. This is meant to be ZDOTDIR or ZSH home dir. It is important to copy over or symlink `.zshenv` to `~` like:
```bash
ln -s ~/dotfiles/zsh/.zshenv ~/.zshenv
```
See load order in files.
