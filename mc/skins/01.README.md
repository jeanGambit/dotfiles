# Instruction to  install theme/skin
Install skin under `$HOME/.local/share/mc/skins` using link:

```bash
mkdir -p ~/.local/share/mc/skins && ln -s -f ~/dotfiles/mc/skins/catppuccin.ini ~/.local/share/mc/skins/catppuccin.ini
```
